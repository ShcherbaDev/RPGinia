function controller(worldClass) {
    // const frontColumns = worldClass.getElementsFromLayer(1);

    // function adf() {
    //     worldClass.getElementByName("corridor").coords[0] -= 5;
    //     for(let i in frontColumns) {
    //         frontColumns[i].coords[0] -= 8;
    //     }
    //     requestAnimationFrame(adf);
    // }
    // adf();
}